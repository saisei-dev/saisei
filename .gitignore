.DS_Store
build/
images/freedos.img
autoheal.log
save_restore.log
# Runtime crash bundles emitted by the shim layer at run time.
crashes/
# Game bundles are local-only (copyrighted) — the repo ships no games; only
# games/README.md is tracked. `new-game` populates games/<name>/ on your machine.
games/*/
# Rust toolchain build output + local Capstone link config.
target/
.capstone/
